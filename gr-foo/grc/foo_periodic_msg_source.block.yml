# auto-generated by grc.converter

id: foo_periodic_msg_source
label: Periodic Message Source
category: '[Foo]'
flags: [python, cpp]

parameters:
-   id: msg
    label: Message PMT
    dtype: string
    default: Hello World!
-   id: interval
    label: Period (ms)
    dtype: int
    default: '1000'
-   id: quit
    label: Quit
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: num
    label: Num Messages
    dtype: int
    default: '-1'

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: |-
        import foo
        import pmt
    make: foo.periodic_msg_source(pmt.intern(${msg}), ${interval}, ${num}, ${quit}, ${debug})

cpp_templates:
    includes: ['#include <foo/periodic_msg_source.h>']
    declarations: 'foo::periodic_msg_source::sptr ${id};'
    make: 'this->${id} = foo::periodic_msg_source::make(pmt::intern(${msg}), ${interval}, ${num}, ${quit}, ${debug});'
    link: ['gnuradio-foo']

file_format: 1
