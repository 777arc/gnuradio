# auto-generated by grc.converter

id: foo_burst_tagger
label: Burst Tagger
category: '[Foo]'
flags: [python, cpp]

parameters:
-   id: tag_name
    label: Tag Name
    dtype: string
    default: burst_len
-   id: mult
    label: Multiplier
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
asserts:
- ${ mult > 0 }

templates:
    imports: |-
        import pmt
        import foo
    make: foo.burst_tagger(pmt.intern(${tag_name}), ${mult})

cpp_templates:
    includes: ['#include <foo/burst_tagger.h>']
    declarations: 'foo::burst_tagger::sptr ${id};'
    make: 'this->${id} = foo::burst_tagger::make(pmt::intern(${tag_name}), ${mult});'
    link: ['gnuradio-foo']

file_format: 1
