# auto-generated by grc.converter

id: foo_rtt_measure
label: RTT measure
category: '[Foo]'
flags: [python, cpp]

parameters:
-   id: interval
    label: Send interval
    dtype: int
    default: '500'

inputs:
-   domain: message
    id: in

outputs:
-   label: dout
    domain: stream
    dtype: byte
-   domain: message
    id: out

templates:
    imports: import foo
    make: foo.rtt_measure(${interval})

cpp_templates:
    includes: ['#include <foo/rtt_measure.h>']
    declarations: 'foo::rtt_measure::sptr ${id};'
    make: 'this->${id} = foo::rtt_measure::make(${interval});'
    link: ['gnuradio-foo']

file_format: 1
