# auto-generated by grc.converter

id: foo_wireshark_connector
label: Wireshark Connector
category: '[Foo]'
flags: [python, cpp]

parameters:
-   id: tech
    label: Technology
    dtype: enum
    default: '127'
    options: ['127', '195', '283']
    option_labels: [WiFi, ZigBee, ZigBeeTap]
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]

inputs:
-   domain: message
    id: in

outputs:
-   domain: stream
    dtype: byte
    multiplicity: '1'

templates:
    imports: import foo
    make: foo.wireshark_connector(${tech}, ${debug})

cpp_templates:
    includes: ['#include <foo/wireshark_connector.h>']
    declarations: 'foo::wireshark_connector::sptr ${id};'
    make: 'this->${id} = foo::wireshark_connector::make(static_cast<foo::LinkType>(${tech}), ${debug});'
    link: ['gnuradio-foo']

file_format: 1
