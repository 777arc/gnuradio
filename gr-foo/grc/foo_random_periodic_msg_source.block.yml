# auto-generated by grc.converter

id: foo_random_periodic_msg_source
label: Random Periodic Message Source
category: '[Foo]'
flags: [python, cpp]

parameters:
-   id: msg_len
    label: Message Length
    dtype: int
    default: '64'
-   id: interval
    label: Period (ms)
    dtype: int
    default: '1000'
-   id: quit
    label: Quit
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: num
    label: Num Messages
    dtype: int
    default: '1'
-   id: seed
    label: Random Seed
    dtype: int
    default: '1'

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: |-
        import foo
        import pmt
    make: foo.random_periodic_msg_source(${msg_len}, ${interval}, ${num}, ${quit},
        ${debug}, ${seed})

cpp_templates:
    includes: ['#include <foo/random_periodic_msg_source.h>']
    declarations: 'foo::random_periodic_msg_source::sptr ${id};'
    make: 'this->${id} = foo::random_periodic_msg_source::make(${msg_len}, ${interval}, ${num}, ${quit}, ${debug}, ${seed});'
    link: ['gnuradio-foo']

file_format: 1
