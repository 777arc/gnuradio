# auto-generated by grc.converter

id: foo_packet_dropper
label: Packet Dropper
category: '[Foo]'
flags: [python, cpp]

parameters:
-   id: drop_rate
    label: Packet drop rate
    dtype: real
    default: '0.5'
-   id: seed
    label: Random seed
    dtype: int
    default: '42'

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import foo
    make: foo.packet_dropper(${drop_rate}, ${seed})

cpp_templates:
    includes: ['#include <foo/packet_dropper.h>']
    declarations: 'foo::packet_dropper::sptr ${id};'
    make: 'this->${id} = foo::packet_dropper::make(${drop_rate}, ${seed});'
    link: ['gnuradio-foo']

file_format: 1
