# auto-generated by grc.converter

id: rds_parser
label: RDS Parser
category: '[RDS]'
flags: [python, cpp]

parameters:
-   id: log
    label: Log
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: pty_locale
    label: PTY Locale
    dtype: int
    default: '0'
    options: ['0', '1']
    option_labels: [Europe, North America]
-   id: reset
    label: Reset
    dtype: int
    default: '0'
    hide: part

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: import rds
    make: rds.parser(${log}, ${debug}, ${pty_locale})
    callbacks:
    - 'reset() # ${reset}'

cpp_templates:
    includes: ['#include <rds/parser.h>']
    declarations: 'rds::parser::sptr ${id};'
    make: 'this->${id} = rds::parser::make(${log}, ${debug}, ${pty_locale});'
    link: ['gnuradio-rds']

file_format: 1
